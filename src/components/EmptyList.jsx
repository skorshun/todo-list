const EmptyList = () => {
    return (
        <div className="empty">
            <p className="empty__title">No tasks yet</p>
            <p className="empty__text">Add the first one — and let's go! 🚀</p>
        </div>
        )
}

export default EmptyList